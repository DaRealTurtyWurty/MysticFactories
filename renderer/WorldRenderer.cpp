#include "WorldRenderer.h"

WorldRenderer::WorldRenderer(World* pWorld) : world(pWorld) {}

void WorldRenderer::Render(SDL_Renderer* renderer) {
    // TODO: Go through nearby chunks and render out the tiles and entities
    // TODO: Render the player
}
